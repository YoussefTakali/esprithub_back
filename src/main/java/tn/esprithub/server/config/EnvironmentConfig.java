package tn.esprithub.server.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class EnvironmentConfig {
    // This class is intentionally left blank.
    // The application is configured to load environment variables from a .env file
    // using the spring-dotenv library, which automatically loads the .env file
    // at the correct time for Spring property resolution.
}
