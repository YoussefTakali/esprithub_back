package tn.esprithub.server.project.enums;

public enum TaskAssignmentType {
    INDIVIDUAL, // Assigned to a single student
    GROUP,      // Assigned to a group
    PROJECT,    // Assigned to all groups in a project
    CLASSE      // Assigned to all groups in a class (for a project)
}
